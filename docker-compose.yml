services:
  judy-space:
    build: .
    image: judy-space:latest
    container_name: judy-space
    restart: unless-stopped
    ports:
      - "8090:80"
    volumes:
      - judy-space-data:/data

volumes:
  judy-space-data:
